# G ONE AI — Gemini API Edition

G ONE AI uses Google's Gemini API through a local Node.js backend. The API key stays server-side in `.env`.

Default model: `gemini-2.5-flash-lite`.

Start with `START_G_ONE_AI_GEMINI.bat` or use `npm.cmd install` then `npm.cmd start`.
